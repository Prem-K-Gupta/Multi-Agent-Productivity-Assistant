# Gemini API Key (get from https://aistudio.google.com/apikey)
GEMINI_API_KEY=your_gemini_api_key_here

# Google OAuth2 credentials (download from Google Cloud Console)
# Place the downloaded JSON as backend/credentials.json
# On first run, a browser will open for OAuth consent.
# Token is saved to backend/token.json for reuse.
